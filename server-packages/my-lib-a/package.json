{
    "name": "@restfulhead/my-lib-a",
    "version": "1.0.0",
    "description": "",
    "main": "dist/index.js",
    "typings": "dist/index.d.ts",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "rm -rf ./dist/ && rm -f tsconfig.tsbuildinfo && tsc --build",
        "buildfast": "tsc --build --incremental"
    },
    "private": true,
    "dependencies": {
        "@restfulhead/my-lib-b": "1.0.0"
    },
    "devDependencies": {
        "@types/node": "~14.14.31",
        "ts-node": "~10.0.0",
        "typescript": "~4.3.5"
    }
}